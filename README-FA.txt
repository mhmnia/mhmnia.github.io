فقط یک فایل را آپلود کن: index.html

GitHub > mhmnia.github.io > Add file > Upload files
index.html را Drag & Drop کن و Commit changes را بزن.

بعد سایت را باز کن و Ctrl + Shift + R بزن.

این نسخه فقط نقشه Study Sites را به صفحه اول فعلی برمی‌گرداند و بقیه تغییرات را دست نمی‌زند.
